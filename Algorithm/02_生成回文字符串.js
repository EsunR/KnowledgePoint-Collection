// 给定一个字符串s，你可以从中删除一些字符，使得剩下的串是一个回文串。如何删除才能使得回文串最长呢？
// 输出需要删除的字符个数。

function SpliceBackWord(str) {
  let strArr = str.split("");
  for (let i = 0; i < strArr.length / 2; i++) {
    
  }
}

























